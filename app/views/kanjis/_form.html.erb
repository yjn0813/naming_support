<%= form_for(@kanji) do |f| %>
  <% if @kanji.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kanji.errors.count, "error") %> prohibited this kanji from being saved:</h2>

      <ul>
      <% @kanji.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :kanji %><br>
    <%= f.text_field :kanji %>
  </div>
  <div class="field">
    <%= f.label :kakusu %><br>
    <%= f.number_field :kakusu %>
  </div>
  <div class="field">
    <%= f.label :yomi %><br>
    <%= f.text_field :yomi %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
